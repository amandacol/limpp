<style>
  h4{
    color: #333132;
    font-weight: bold;
    font-family: 'Montserrat', sans-serif;
    }
  p{
    color: #707070;
    font-family: 'Montserrat', sans-serif;
    }
    .btn-avaliacao{
      color: white;
      background-color: #8F89DD;
      border-radius: 30px;
      padding: 0.5rem 2rem;
      font-weight: bold;
      font-family: 'Montserrat', sans-serif;
      font-size: 13px;
      box-shadow: 0px 2px 2px #33313261;
    }
</style>
<div class="container-fluid mt-4">
  <div class="d-flex justify-content-center"><%= image_tag "barcode.svg" %></div>
    <h4 class="text-center mt-5">Ainda não avaliamos este produto.</h4>
    <p class="text-center mt-5">Para sugerir a avaliação deste produto, clique no botão abaixo e envie as fotos do rótulo para nossa equipe.</p>
    <div class="d-flex justify-content-center">
    <% if policy(Newitem).create? %>
        <%= link_to "Sugerir avaliação", new_newitem_path, class: "btn btn-avaliacao mt-4"%>
    <% end %>
    </div>
</div>
